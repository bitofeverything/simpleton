import commands from './commands';

const logic = {};

class MapFunctions extends Object {

  // idea being, ingest commands and setup the initial mapping for everything

  beginPersistent() {
    // initialize all persistent functions
  }

  //
}
